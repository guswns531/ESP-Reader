# Math Notes

수식은 아직 완전 렌더링하지 않습니다.

하지만 문서 구조와 문맥은 유지하는 방향으로 갑니다.

## Plan

1. Markdown subset 안정화
2. 긴 문서 탐색 개선
3. 수식 block 처리 전략 확정

Inline math 예시:

$E = mc^2$

Block math 예시:

$$
\int_0^1 x^2 dx = 1/3
$$

Code block 예시:

```text
math mode is not rendered yet
but it is visually separated
from normal paragraph text
```

> 수식은 아직 계산되지 않지만 블록으로 구분된다.

---

문장 안의 `x^2` 같은 인라인 코드는 대괄호 형태로 보이게 된다.
